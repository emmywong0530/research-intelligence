# Post-Task-3B Integration Checkpoint

## Run identity

- Repository: `emmywong0530/research-intelligence`
- Branch: `feature/m3b-research-profile`
- Commit: containing Task 3B implementation; final SHA is reported after commit
- Date: `2026-07-19`
- Host: macOS arm64 sandbox
- Workspace data: disposable temporary workspaces only; no real user workspace
- Scope: post-Task-3A/Task-3B checkpoint, with inherited Task 0 and Task 2 evidence

## Environment

- Python: companion virtual environment Python 3.14.6
- Node: bundled Codex Node runtime
- pnpm: 11.9.0
- Companion packaging: local PyInstaller 6.21.0 build attempted on macOS arm64
- Browser: Chromium/Playwright browser phase unavailable locally; Vite preview
  and HTTPS spike servers were also blocked from binding loopback ports
- Network: npm audit registry access unavailable

## Evidence summary

| Checkpoint | Result | Evidence |
|---|---|---|
| Schemas and static quality | Pass | 9 schemas, frontend lint/typecheck/build, Ruff, 64 companion tests |
| Task 3B API lifecycle | Pass locally | 5 real FastAPI tests cover create/read/update/list/reopen, identity, schema rejection, auth/origin, and project isolation |
| Task 3B frontend lifecycle | Pass with mocked fetch | 11 focused profile tests and 32-test frontend suite; no browser claim |
| Existing Task 0 security foundation | Inherited local/CI evidence | Existing security and API suites remain green; no security middleware was weakened |
| Browser PWA/companion path | Unverified locally | `frontend:e2e` failed loopback bind with `EPERM`; `spike:pwa-loopback` failed HTTPS bind with `EPERM` and host `uvicorn` missing |
| Dependency audit | Unverified | `pnpm audit` failed because npm registry fetch returned `ENOTFOUND` |
| Packaging | Unverified locally | PyInstaller collection reached but sandbox denied its cache directory; no artifact was claimed |

## Task 3B integration paths

The authenticated companion tests used a temporary workspace and the real
FastAPI generic record routes. They verified the profile path
`projects/<project-id>/research-profile.json`, deterministic identity,
schema-backed writes, revision conflicts, duplicate prevention, reopen
persistence, exact origin checks, missing-origin rejection, and project
association enforcement.

The frontend tests verified project selection, project-required state, explicit
profile creation, supported-field editing, duplicate/weight validation,
preservation after errors, dirty reload protection, project isolation, conflict
reconciliation, and no browser storage writes. These tests use mocked fetch and
therefore are not browser end-to-end evidence.

The complete browser path, including PWA load, semantic companion connection,
onboarding, pairing, workspace setup, project selection, profile create/save,
reload, and conflict interaction remains unverified locally. No GitHub Actions
run for this Task 3B branch was available while recording this checkpoint.

## Security and privacy

No API, schema, pairing, keychain, storage, or packaging behavior was weakened.
The profile uses existing authenticated generic routes and keeps session data
in memory. No secret, credential, private paper, unpublished material, or real
workspace was committed.

## Feature states

- Task 3B companion profile lifecycle: `Locally persisted` based on the real
  disposable-workspace API tests.
- Task 3B frontend profile lifecycle: `Companion connected` for mocked UI
  state coverage; the real browser path is unverified.
- Full user journey across browser, PWA, companion, and restart: not
  end-to-end verified.
- No capability is `Production ready`.

## Merge blockers

No locally executable Task 3B API or frontend-unit blocker was found. The
blocked browser, audit, and packaging commands are evidence gaps and must not
be described as passes. A release or end-to-end status must wait for executable
browser and cross-platform packaging evidence.

## Follow-up improvements

- Run the HTTPS static-host profile journey in GitHub Actions with Chromium.
- Capture profile editor visual states and inspect accessibility at the approved
  responsive sizes.
- Rerun dependency audit and packaging in a network-enabled, unrestricted
  environment.
